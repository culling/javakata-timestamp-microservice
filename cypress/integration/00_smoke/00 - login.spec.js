// /// <reference types="cypress" />
// import adminUser from '../../../cypress/fixtures/users/adminUser.json';

// describe('login to jira', () => {
//   it("should login to jira", () => {
//     cy.jira(Cypress.config().baseUrl).then((jiraHelper) => {
//       jiraHelper.login(adminUser);
//     });
//   });
// });
